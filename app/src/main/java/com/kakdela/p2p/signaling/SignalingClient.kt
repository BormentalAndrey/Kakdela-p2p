package com.kakdela.p2p.signaling

// Заглушка для сигнализации (для WebRTC через QR или P2P)
class SignalingClient {
    fun sendOffer(sdp: String) {
        // Отправка offer через QR или P2P
    }
}
